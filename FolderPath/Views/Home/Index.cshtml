@model string

@{
    ViewData["Title"] = "Home Page";
    // TODO ---> check if we have extra '/' in the end 
    var cleanedPath = @Model is null ? "" : @Model;
}

<div class="text-center">
    <a href="@(cleanedPath)/root">Root</a><br>
    <a href="@(cleanedPath)/level11">Level 1.1</a><br>
    <a href="@(cleanedPath)/level12">Level 1.2</a><br>
    <a href="@(cleanedPath)/level13">Level 1.3</a><br>
</div>